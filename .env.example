# =================================================================
# NetDevOps Project 2 - Unified Environment Configuration
# =================================================================
# INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual credentials
# 3. NEVER commit .env to version control
# 4. Ensure .env is in .gitignore
# =================================================================

# ┌─────────────────────────────────────────────────────────────┐
# │ NetBox Configuration (Source of Truth)                      │
# │ Used by: bulk_ip_assign.py, bulk_provision.py,             │
# │          get_slugs.py, health_poller.py                    │
# └─────────────────────────────────────────────────────────────┘
# NetBox API endpoint - typically http://<host>:8000
NETBOX_URL=http://100.89.136.43:8000

# NetBox API token - generate from NetBox UI (Admin > API Tokens)
NETBOX_TOKEN=your_netbox_api_token_here

# ┌─────────────────────────────────────────────────────────────┐
# │ InfluxDB Configuration (Time-Series Database)               │
# │ Used by: docker-compose.yml (initialization),              │
# │          health_poller.py (runtime),                       │
# │          verify_stack.py (verification)                    │
# └─────────────────────────────────────────────────────────────┘
# InfluxDB initialization credentials (used during docker-compose setup)
# These values are set ONCE when the container first starts
INFLUXDB_USERNAME=admin
INFLUXDB_PASSWORD=your_secure_password_here
INFLUXDB_ORG=NetDevOps
INFLUXDB_BUCKET=NetworkHealth

# InfluxDB runtime connection credentials
# INFLUX_URL: API endpoint where health_poller.py connects
INFLUX_URL=http://localhost:8086

# INFLUX_TOKEN: API token - generate from InfluxDB UI (Data > API Tokens)
INFLUX_TOKEN=your_influxdb_api_token_here

# INFLUX_ORG: Organization name (should match INFLUXDB_ORG above)
INFLUX_ORG=NetDevOps

# ┌─────────────────────────────────────────────────────────────┐
# │ Polling Configuration (Optional)                            │
# └─────────────────────────────────────────────────────────────┘
# Interval in seconds between health polls (default: 30)
POLL_INTERVAL=30
